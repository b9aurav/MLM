{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Run Script: ng serve",
            "request": "launch",
            "command": "npm run ng serve",
            "cwd": "${workspaceFolder}"
        }
    ]
}